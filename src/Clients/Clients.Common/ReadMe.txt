Client should be 1 single self-contained dll. 
Ok. It will depend on JSON.Net. But we don't want too many dependencies.

It does not reference any other project. However, it needs code from other projects.
It is going to get references to that code via the "Add as link" feature.

